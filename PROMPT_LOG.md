# Team AI Prompt & Audit Log

**Project Name:** Wasted
**Team Members:** Andres Garcia Gonazlez, Zantiago Vargas Beltran, Pedro Chucuan Gerardo
**SDG Goal Target:** Goal 12: Responsible Consumption and Production (partially Goal 11: Sustainable Cities and Communities)
**Scope:** Team project. Individual classwork is logged in `Classwork/PROMPT_LOG.md`.

---

## Rules of AI Engagement (quick reference)

1. **Boilerplate is fine; architecture is yours.** AI may generate layouts, CSS/Bootstrap grids, regex, SQL/Mongoose schemas, or boilerplate fetch functions. Routing, state trees, and integration logic must be understood by us.
2. **Every prompt gets a human audit.** No blind copy-paste: review, test, refine, and document what we changed.
3. **Oral defense readiness.** Any AI-generated block can be pointed at during evaluation. We must explain how it works and how it was integrated. This log is the study guide.

**Writing rows:** paste the prompt word for word. If it contains a `|`, escape it as `\|`; for line breaks inside a cell use `<br>`.

---

## Week 1

_Add one row per prompt. If no AI was used this week, write: "No Generative AI was used in Week 1."_


---
| Goal | Prompt | Audit | Learnings |
| :--- | :--- | :--- | :--- |
| Rework the proposal so it answers the four objections the professor raised. | "Eres el arquitecto encargado del desarrollo de mi proyecto. Quiero que primero me ayudes a hacer algunas correcciones exactamente en la propuesta de mi proyecto, que puedes encontrar en wasted.html.<br><br>el profesor nos dio varios puntos. Te lo anoto a continuación y te anoto también cómo es que vamos a resolver estos problemas en nuestro proyecto.<br><br>Nice clear idea. Yet I wonder how would this keep the user engagement or how to exploit the results gotten. One limitation for this app would be the location rules, that could be tricky because maybe there is not easily accessible information about that, you may need to create such DB. One idea for growth would be that once you have enough information from a user, you may be able to suggest consumption improvements by visualizing their waste patterns and offering alternatives. Make sure that your interaction with the CV API works properly for your purposes, I don't know if there is already a data set for this purposes.<br><br>Nuestra primera respuesta es que habrá streaks: cuando un usuario hace al menos 5 consultas todos los días, va ganando un streak del día. Mientras más streaks hagas en un día, puedes acumular otros premios. Podrían serlos marcas que quieran apoyar este proyecto, alguna marca ecológica que quiera promover esto, o el gobierno podría darnos a nosotros ciertos incentivos.<br><br>Un ejemplo podría ser que Procter &amp; Gamble se interese por el medio ambiente. Cuando lleves utilizando nuestra app y consultando sobre desechos más de 20 días consecutivos en un streak, te damos un cupón de descuento para que compres tu próximo lavatrastes ecológico. Todo en Waste tiene que ser ecológico y ayudar a reducir la marca de carbono. Para las location rules, investigan la web, pero según yo hay una página web o una base de datos del gobierno a la que podemos acceder, donde tienen todos sus diferentes centros de desecho y reciclaje. Para que entonces hagamos fetch a la ubicación del usuario con su permiso, identifiquemos el tipo de desecho y le demos una ruta para que pueda verla en Waze o Google Maps (para saber a dónde se tiene que dirigir para hacer dispose de este desecho de la manera responsable y así ganar su streak). Además, una vez que tengamos algunos datos sobre su consumo, podemos sugerirle ciertas alternativas ecológicas, como no sugirió el profesor. Sin embargo, esto va a ser una feature muy básica y muy mínima, ya que para poder predecir el consumo vamos a tener que meternos en temas más complejos. Para resolver el problema de los datos, lo que haremos es que el usuario tome una foto. Nosotros enviamos esa foto a un LLM que tenga visión. El LLM la procesa y nos va a responder en un formato JSON específico que diga qué es, qué tipo de desecho es, dónde se tira, etcétera.<br><br>Nosotros procesamos esa información, buscamos dónde desecharlo y mostramos todo en la UI. Es un proceso directo end to end." | The four answers are ours, not the AI's: streaks with brand rewards, the photo → vision LLM → JSON pipeline, the government catalogue of collection centres, and keeping the consumption-pattern idea deliberately minimal. The AI wrote them into the HTML. Two follow-ups were needed. First the shape: "Con todo esto en mente, en el HTML solamente agrega una sección que diga “Observaciones del profe”, donde ponemos la observación que nos dio el profe, cómo nosotros la tomamos en cuenta y cómo la vamos a resolver." Then the length, because the answer on the data source ran long and cited nothing: "Hay que resumir el punto 3, ya que lo estás contestando demasiado extensamente. Ve más directo al grano y cita de dónde vamos a sacar la info." | Each objection sits next to our answer in the same section, so the professor reads the pair instead of hunting for it. The privacy rule falls out of this design: the photo only travels to the vision model, and what we keep is the JSON it returns, which is why the proposal can say the picture is not stored by default. |
| Find a real, fetchable source for the collection centres in Jalisco. | "Ahora, sobre el último punto, las ubicaciones de los centros de desecho: quiero que despliegues un agente de Teams para investigar a profundidad en internet este recurso.<br><br>Recuerdo que, cuando estaba viendo la viabilidad técnica anteriormente, de alguna manera había una base de datos que nadie utilizaba. Decía qué centros de reciclaje tienen y todo esto. Investiga para tener la lista exacta y ver de dónde vamos a hacer fetch, al menos en Jalisco, de los centros de reciclaje que vamos a ocupar." | Two candidate sources were rejected during the research: `datos.jalisco.gob.mx` was down, and the GeoServer layer we had assumed held municipal centres turned out to be registered private companies. We made it verify that second reading before accepting it. What survived: the SEMADET layer on ArcGIS Online as the source, and INEGI's DENUE as the fallback. | The matching step compares the JSON the model returns against that catalogue, so our category keys have to be the catalogue's keys. A source that answers today is not a source we control, which is why the fallback is written down next to it. |
| Design the Wasted isotype: a W merged with a bin, to generate it with an image AI. | "[Image #1] Estamos trabajando en este proyecto de Wasted y tengo que hacer un logo. Específicamente estamos buscando hacer un isótipo para que reemplace la letra W por una integración entre un W y un icono de basura muy minimalista, con pocos trazos y tipo SVG vectorizado personalizado.<br><br>Lo generaré con un AI LLM de generación de imágenes. Analízalo, ve mi propuesta, imagínatelo y luego dame el prompt para que yo lo pueda copiar en mi AI generative image, de preferencia." | The AI only wrote the text prompt for the image generator; it did not draw the logo. The concept (the W *is* the bin, few strokes) was ours, and so was picking and cleaning the final SVG. The three files in `design-system/` are the result. | The isotype and the letter are the same stroke, which is why the logo cannot be redrawn in another typeface. The viewBox ratios (`0 0 339 116` for the lockup, `0 0 72 78` for the icon) are what every later layout sizes against. |
| Turn the logos and palette we chose into a design system doc, and rebrand the proposal HTML with them. | "Te agregué los dos logos para el proyecto, además de la paleta de colores. Quiero que la extraigas y que la pongas en un brief system design MD para que tengamos los colores y los dos logos SVG, para que podamos hacerlo y referenciarlo como nuestro system design mucho más adelante. Con esto en mente, rediseña el wasted HTML que tenemos, cambia los colores y reemplaza el logo que tienes por el verdadero logo SVG. [Image #2]" | The five greens and the logos were our decision; the AI extracted them into `design-system/DESIGN_SYSTEM.md` and applied them to `wasted.html`. Measuring the contrast was the useful part: it flagged that `--sage` on white is only 2.63:1, so we restricted it to rules and borders, never text, and that `--moss` on `--lime` fails AA at 4.46. | The doc is the single source and the HTML only consumes its tokens, so a color change happens in one place. WCAG exempts logotypes from contrast, which is why the lockup still needs `alt="Wasted"` and is never used as running text. |
| Add a closing hero section with the big logo and the slogan. | "Cierre abajo del todo. Quiero que pongas una sección de cierre tipo hero con el logo muy grande y un eslogan que diga "Throw it right"." | It took three corrections. First we asked for the shape: "ponlo en una card flotante, todo center aligned, rounded corners." It then sized the card to the text instead of the layout, so we said "debe ser del ancho de la columna central que tenemos (para que se vea diseno consistente.." and, when that still was not it, "Prefiero que esté alineado a todo el ancho de la columna central, no solamente el texto." Only then did it use `grid-column: 1 / -1`. | The card is the one block that spans both columns of the grid; everything else stays in the content column. Saying "full width" is ambiguous to the model: the fix was naming the grid behaviour we wanted, not the look. |
| Export the proposal to a single continuous PDF. | "Ahora prepara un script para exportarlo a PDF, display scale de 1. (todo en una sola page..)" | The first version came out as six A4 sheets, because `page.pdf()` applies the `@media print` CSS. It needed `page.emulateMedia({ media: "screen" })`. | `export-pdf.mjs` measures `scrollHeight` and asks Chrome for a page exactly that tall, which is how the document stays in one piece. The script also counts the pages in the output and exits with an error if there is more than one. |
| Split the workspace into folders so the design system, the deliverables and the app each live in one place. | "Organiza todo el workspace en donde dividas un folder para el design system y otro folder para entregables. Deja el prompt log en el root." | It moved the files but left `wasted.html` pointing at the old logo locations, so the four `src`/`href` had to be rewritten. The folder names and the rule that `entregables/` only holds what we submit were ours. We re-ran the export to confirm the logos still resolved. | Relative paths belong to the file, not to the folder: moving an HTML breaks every asset link inside it. The layout is `design-system/` (brand source), `entregables/` (what we submit), `wasted/` (the app) and `PROMPT_LOG.md` at the root. |
| Decide which modules the app has, and argue each one. | "Eres mi arquitecto tecnico, tu trabajo es analizar los requirements del proyecto y debatir conmigo para llegar a definir que modulos debera tener mi webapp, puedes encontrar un briefing de la misma en @entregables/ Basado en este briefing, ayúdame a debatir conmigo qué módulos debería tener mi aplicación y por qué, con una justificación breve y no mayor a un renglón. No te desvíes a tratar de pensar en business scaling ni en otro tipo de features. Solamente haz el core, y hazme una propuesta de que deberiamos tener y porque (piensa que cada modulo es un boton que se vera en la navbar).. Investiga en la web si es necesario para que comprendas como las apps determinan que modulos seran necesarios." | Its proposal included "Buscar" and "Dónde tirarlo" as modules of their own. We cut both and told it why: "buscar no lo necesitamos, podemos hacer un fallback en escanear, si no hay foto, un boton te abre un text-chat (...) el menu de donde tirarlo no es necesairo, ya que este sera un sub menu del modulo de escaneo". The hierarchy is ours too: Scan as the sphere at the far right, Profile and History in the pill on the left, History closest to the thumb because it is touched more than Profile. We also handed it the five-step flow the layout had to fit. | A module is a place you navigate to; anything that only appears after an action is a screen, not a module. Result has two entrances, a fresh scan and a card reopened from History, and behaves differently in each, which is why it is a shared screen instead of a sixth button. |
| Build the first delivery: five screens, the navigation bar and the Bootstrap grid, with no functionality yet. | "PET, vidrio, aluminio, carton, papel, organico, electronicos y peligrosos POR AHORA, luego determinamos mas (para hacer el UI/UX y layout aun no necesito toda esa info (solo dejalo escrito en el contexto de proyecto para el PRD mas adelante). El primer entregable, el dia de hoy es solo dejar el foundation, especificamente:<br><br>Prototype. This delivery should include the basic structure of the whole project. The content and the layout of the project should be all in place or at least highlighted. At this point the navigation and functionality is not expected to be fully there, but it should give me a clear idea of how it is going to work. This delivery should include:<br>Adaptable website in HTML5 suitable for wide and mobile view at least.<br>It should use the bootstrap grid system.<br>Reusable elements should be in place.<br>Design based on topics described in class.<br>Consistent color palette.<br>File organization using external styles (although if required limited internals can be used).<br><br>This delivery should be accessible via github pages, thus at this point you should know how to do so. The delivery will be done via a link to your repo and the published public accesible website.<br><br>The website should already contain the menu component displaying all the sections of the final project and all the sections should at least have a projected look-and-feel of the page.<br><br>Submit the repo url AND the url to your github page." | The eight categories are ours and we told it to stop there rather than invent more. We also held it to the grid only: the CDN link is `bootstrap-grid.min.css`, not the whole framework, because every component on screen is our own CSS. The icon sprite and the bottom nav are duplicated verbatim in all five HTML files on purpose, since this delivery has no build step and no framework to share them with. | The eight categories appear in three places that have to move together: the illustrations we model, the enum in the JSON the LLM returns, and the keys we match against the SEMADET catalogue. `bootstrap-grid.min.css` gives the twelve-column responsive grid without dragging in components that would fight our palette. |
| Redesign the five screens around contrasting surfaces instead of borders, with icons and half the text. | "[Image #4] Basate en este ejemplo de navbar para mejorar la nuestra ,el boton principal debe tener un gradiente sutil y un 3d shading (search  on web como se hace el 3d shading effect con CSS), ademas en todo el diseno hay que quitar las eyebrows, el border stroke en todos lados, y dejar un layout de bentogrids consistentes y bien alineado (todo siempre debe alinear bottom-align de ser necesario. Utiliza iconos para mejorar la escaneabilidad del usuarioy reduce 50% el texto que ve el usuario, ej: en el scan, no tienes que mostrar la descripcion de cada paso, con describir lo que hacemos en step by step con iconos basta. Los disclamers icual deben llevar un color distintivo, y un icono de "i (info)". svg y en lugar de usar cards con border strock, utiliza cards con un background color contrastante WCAG approved para organizar y agrupar componentes o controles que tengan semantca similar o pertenezcan al mismo grupo. (despues utiliza la skill de visual check)." | It took two rounds of corrections, both ours and both naming the behaviour instead of the look. Layout: "Mueve el div-w-btnrow arriba de "scan", cambiemos el label de "scan" a steps", ademas agreguemos mas spacing overall, especificamente entre el header-topbar y los elementos inferiorres, demos mas "margin bottom" para mantener la sensacion de minimalismo, y espacio." Colour: "cambiemos el background de estos botones en lugar de lime, a ser el dark green que usamos en los CTAs (como el boton de la camara). Y en el navbar cuando estemos en el HTML de "camara" (sea esta el active tab).. hagamos el boton scale 25% para notar en donde estamos (e invierte colores)." We also checked its surface pairs against `DESIGN_SYSTEM.md`: `--moss` as text on `--lime` is only 4.46, so the notices are white text on a moss block (5.24) instead. | Grouping by surface instead of by border means every block needs a text colour that passes AA against it, so the four allowed pairs are listed at the top of `wasted.css` and nothing else is used. The icons are Lucide paths inlined into one SVG `<symbol>` sprite, which is why they inherit `currentColor` and change colour with the surface they sit on. |
| Warn a desktop visitor to switch the browser into phone emulation. | "En el setup, agreguemos alguna detección para saber si el usuario está en computadora. Si está en computadora, mostramos un modal en el que se explique que debe entrar en modo emulador, inspeccionar y entrar en el emulador de móvil para poder ver el layout y cómo la aplicación debería funcionar. Esto está fuera del scope del proyecto, entonces dejaremos que sea un componente meramente informativo. De esta manera, cuando el profesor revise la aplicación, realmente verá el layout como se verá una vez desplegado." | The first version keyed off window width and never fired. We caught it by opening the page ourselves: "Yo lo probé y lo estoy viendo desde la computadora y no me mostró en ningún momento el warning." The test in `viewport-tip.js` was changed to `matchMedia("(hover: hover) and (pointer: fine)")`, which asks whether the machine has a mouse rather than how wide the window is, because a laptop with a narrow window is still a computer and that is exactly when the notice matters. | Device emulation reports a coarse pointer and no hover, so entering it closes the dialog on its own, which doubles as confirmation the reader did it right. `<dialog>.showModal()` supplies the focus trap, the Esc key and the inert background, so the application does not need to implement those behaviours separately. |
| Match the reference screenshot's glass panel and 3D button using only our static CSS. | "Carga las skills de diseño YoAI y YoPro Max, y el skill para diseñar sitios web con Awards. Quiero que analices esta foto de referencia pixel by pixel para que entiendas cómo se conforma este glassmorphism, el shade más fuerte que tiene de border stroke y el tipo de 3D shading effect muy sutil que tiene el botón primario. Quiero que lo busquemos replicar con el CSS estático que tenemos.<br><br>Intenta hacer por lo menos cinco variantes hasta acercarte lo más posible al resultado de referencia que te di. Después yo juzgaré si nos quedamos con eso o con la versión actual que tenemos. [Image #1]¿Puedes investigar en la web cómo se hace este tipo de efecto glassmorphism liquid glass de Apple en static HTML y CSS, si es necesario, y el 3D shading effect para los botones." | Six takes were built side by side in `wasted/lab-nav.html` next to `lab-ref.png` and each was diffed against the reference pixel for pixel instead of judged by eye. The result contradicted the brief: V4, the real Liquid Glass with `backdrop-filter`, scored 2.47 while the plain radial gradient of V5 scored 2.13, so we shipped V5 and dropped the glass. | The sphere is three layers, all palette colours: an off-centre radial gradient that sets where the light comes from, an inset highlight at the top with an inset shade at the bottom for the bevel, and an `::after` holding the specular highlight so the gradient underneath stays a clean light-to-dark ramp. `backdrop-filter` turned out not to be needed for the look. |
| Text fallback for when the camera cannot name the item. | "Ahora creamos el modal que aparece cuando vas a hacer clic en Typed. Va a ser sencillo: puedes escribir y te da algunos tips de escritura. Lo que estoy esperando es que el usuario describa el tipo de desecho que tiene y después le dé a Submit.. es el fallback por si no se pudo escanear nada" | Three UI issues were identified in screenshots. The dialog grew past the viewport and pushed Submit off screen, fixed with `max-height` plus internal scroll and by moving the icon beside the title instead of above it. Cancel used `w-btn--soft`, which is white on the white dialog, so it needed a new `w-btn--quiet` variant on lime. And `display:flex` on the `<li>` turned every `<b>` into its own flex item, breaking "water bottle" onto lines of its own until the text was wrapped in a `<span>`. We also stripped the `name` off the textarea so what the user writes never reaches the URL. | `required` on the textarea is the whole validation: the browser blocks an empty submit and the form's `action` does the navigation, so `type-it.js` only opens and closes the dialog. A control without a `name` is not submitted, which is how the description stays out of the query string while the form still works. The trigger stays a real link to the result screen, so the flow survives with JavaScript off. |

## Week 2

| Goal | Prompt | Audit | Learnings |
| :--- | :--- | :--- | :--- |
|  |  |  |  |
